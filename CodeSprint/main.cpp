
// Basic Headers For Competitive Programming
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <iomanip> //Needed For Set Precision
#include <cmath>
#include <map>
#include <set>
using namespace std;

// Type Shortcuts (easier to type)
typedef long long ll;
typedef vector<int> vi;
typedef vector<long long> vl;

// Faster Input And Output
#define fast_io()                     \
    ios_base::sync_with_stdio(false); \
    cin.tie(nullptr);

// Macros for Common Operations
#define pb push_back
#define F first
#define S second
#define yes cout << "YES\n";
#define no cout << "NO\n";
#define sort_all(x) sort((x).begin(), (x).end())
#define reverse_all(x) reverse((x).begin(), (x).end())
#define sz(x) (int)(x).size()
#define point(x) cout << fixed << setprecision(x);

// Range of Integer(int) => -10ˆ9 to 10ˆ9
// Range of Long Long Integer(1LL) => -10ˆ18 to 10ˆ18

// Code Your Solution!
void solve()
{
    int n;
    cin >> n;
}

int main()
{
    fast_io();

    int test;
    cin >> test;
    while (test--)
    {
        solve();
    }

    return 0;
}